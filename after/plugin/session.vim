" " when vim is run without args,
" " load a session if present or
" " start tracking a new session
" if argc() == 0
  " if filereadable('Session.vim')
    " silent source Session.vim
  " else
    " Obsession
  " endif
" endif
