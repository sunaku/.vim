Shortcut (visual-split) split window above
      \ vnoremap <silent> <Space>Ow :VSSplitAbove<CR>

Shortcut (visual-split) split window below
      \ vnoremap <silent> <Space>ow :VSSplitBelow<CR>

Shortcut (visual-split) resize window to selection
      \ vnoremap <silent> <Space>=w :VSResize<CR>
