set infercase             " like smartcase, but for completion
set complete-=i           " don't scan included files on <C-N>
set complete-=t           " don't scan the ctags file on <C-N>
set completeopt+=menuone  " show menu even if only 1 item long
set completeopt+=noinsert " don't auto-insert the current item
